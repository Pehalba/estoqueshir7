rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Uso local: basta estar logado — sem restrição por coleção
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
